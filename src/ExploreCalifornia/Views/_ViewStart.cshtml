@*
    The view start view is a view that gets run before every single other view in this site
     which makes it a great place to put any common logic such as setting the layout property
     for every page in the site.
*@
@{
    Layout = "_Layout";
}